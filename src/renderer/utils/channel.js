import Vue from 'vue'

// class Channel {
//     constructor() {
//         this.channel = new Vue()
//     }

//     emit(channel, callback) {
//         this.channel.$emit(channel, callback)
//     }

//     on(channel, callback) {
//         this.channel.$on(channel, callback)
//     }

//     off(channel, callback) {
//         this.channel.$off(channel, callback)
//     }
// }

export default new Vue()